// TODO: Simplified operational timeline (mobile-friendly).
// Show tables as rows, time slots as columns, status blocks, optional sector filter.
// No drag, no resize, no pointer events, no Woki desktop behavior.

import React from 'react';

export const TimelineScreen: React.FC = () => null;
