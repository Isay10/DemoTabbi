// TODO: AI-ready operational insights (rule-based, no chatbot).
// Show tables open too long, pending payment, high occupancy, shift summary.

import React from 'react';

export const AiInsightsScreen: React.FC = () => null;
