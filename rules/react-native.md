# React Native Rules

Use View, Text, Pressable, FlatList, ScrollView, TextInput, Modal and StyleSheet.

Do not use div, span, className styling, document, window, DOM event APIs, Pointer Events, CSS files, SCSS Modules, CSS Grid or Ant Design.

Keep the app Expo Go-compatible unless explicitly changed.
